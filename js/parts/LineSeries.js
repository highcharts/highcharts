(function (H) {
	var extendClass = H.extendClass,
		Series = H.Series,
		seriesTypes = H.seriesTypes;
/**
 * LineSeries object
 */
seriesTypes.line = extendClass(Series);

    return H;
}(Highcharts));
