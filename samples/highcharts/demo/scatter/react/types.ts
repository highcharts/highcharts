export type Athlete = {
    sport: string;
    weight: number;
    height: number;
    continent: string;
};

export type SeriesData = {
    triathlon: [number, number][];
    volleyball: [number, number][];
    basketball: [number, number][];
};
