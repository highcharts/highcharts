import type {
    SampleGeneratorConfig
} from '../../../../tools/sample-generator/config.ts';

export default {
    controls: [{
        path: 'yAxis.reversed',
        value: true
    }]
} satisfies SampleGeneratorConfig;