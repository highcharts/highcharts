Highcharts.chart('container', {
    legend: {
        maxWidth: '20%', // Only allow legend to be up to 20% of chart width.
        borderWidth: 1
    },
    series: [{
        name: '123456789',
        data: [0, 1, 2]
    }]
});
