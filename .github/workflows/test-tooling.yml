name: Test tooling

on:
  pull_request:
    branches: [
      'master',
      'tools/*'
    ]
    paths:
      - 'package.json'
      - 'package-lock.json'
      - 'tsconfig.json'
      - 'test/ts-node-unit-tests/**'
      - 'test/visual-test-samples.js'
      - 'tests/visual/**'
      - 'tests/utils.ts'
      - 'tools/**/*'
      - '.github/workflows/visual-compare-playwright.yml'
      - '.github/workflows/test-tooling.yml'
      - 'tests/internal/*'
  workflow_dispatch:

permissions:
   contents: read

jobs:
  notify:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        node-version: ['lts/*', 'latest']

    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Run test
        run: node --test "./tools/gulptasks-tests/*.mjs"

      - name: Run Node tooling tests
        run: npm run test-node:tooling
