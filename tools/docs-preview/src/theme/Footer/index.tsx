import React from 'react';
import type { Props } from '@theme/Footer';
import { Footer as HighsoftFooter } from 'highsoft-ui';

export default function Footer(_props: Props): JSX.Element {
    return (
        <HighsoftFooter
            host=""
        />
    );
}
